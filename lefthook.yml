pre-commit:
  parallel: true
  scripts:
    "trivy-secrets.sh":
      runner: sh
      tags: security

pre-push:
  commands:
    spotlessCheck:
      glob: "./backend/**/*"
      root: backend
      run: ./gradlew spotlessCheck
