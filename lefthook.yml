pre-commit:
  parallel: true
  scripts:
    "trivy-secrets.sh":
      runner: sh
      tags: security

pre-push:
  jobs:
    - name: Spotless Check on Backend
      root: backend/
      script: spotless-check.sh
      runner: sh
