pre-commit:
  parallel: true
  scripts:
    "trivy-secrets.sh":
      runner: sh
      tags: security

pre-push:
  commands:
    spotlessCheckOnBackendChange:
      root: backend
      run: ./gradlew spotlessCheck
