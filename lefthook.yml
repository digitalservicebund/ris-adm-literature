pre-commit:
  parallel: true
  scripts:
    "trivy-secrets.sh":
      runner: sh
      tags: security

pre-push:
  commands:
    spotlessCheckOnBackendChange:
      root: backend # restrict to changes in the backend folder
      run: |
        ../.lefthook/pre-push/spotless-check.sh
