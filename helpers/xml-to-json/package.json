{
  "name": "xml-to-json",
  "version": "0.0.1",
  "description": "Transforming XML to JSON files",
  "main": "index.ts",
  "scripts": {
    "start": "ts-node index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "simple-xml-to-json": "^1.2.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.5"
  }
}