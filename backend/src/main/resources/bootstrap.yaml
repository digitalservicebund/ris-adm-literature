spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        fail-fast: true
        sources:
          - name: database-config
          - name: oauth2-config
          - name: s3-config
          - name: s3-buckets-credentials-secrets
      secrets:
        enabled: true
        fail-fast: true
        paths: /etc/secrets
