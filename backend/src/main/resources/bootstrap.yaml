spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        fail-fast: true
        sources:
          - name: database-config
          - labels:
              spring-config: "true"
