spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        fail-fast: true
        labels:
          spring-config: "true"
      secrets:
        enabled: true
        fail-fast: true
        paths: /etc/secrets
