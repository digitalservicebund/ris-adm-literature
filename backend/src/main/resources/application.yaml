spring:
  application.name: ris-adm-vwv-backend
  datasource:
    #url: jdbc:postgresql://${database.host:localhost}:${database.port:5432}/${database.database:ris-adm-vwv}?currentSchema=${database.schema:adm-vwv}
    url: jdbc:h2:mem:testdb
    username: ${database.user:sa}
    password: ${database.password:}
    #driver-class-name: org.postgresql.Driver
    driver-class-name: org.h2.Driver

management:
  endpoint:
    health:
      probes:
        enabled: true